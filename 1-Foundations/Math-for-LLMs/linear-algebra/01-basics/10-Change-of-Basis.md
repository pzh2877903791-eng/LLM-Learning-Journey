# 🎬 第10集：基变换

## 📺 视频信息
- **视频标题**：Change of basis
- **3B1B原视频**：https://www.youtube.com/watch?v=P2LTAUO1TdA
- **B站中字幕**：https://www.bilibili.com/video/BV1ys411472E?p=10

## 🎯 核心概念

### 1. 什么是基变换？
**核心思想**：同一个向量在不同坐标系（基）下的不同表示

**类比**：
- 同一个人在不同语言中的名字
- 同一个地点在不同地图上的坐标
- 同一组数据在不同特征空间中的表示

### 2. 数学表达
**给定**：
- 标准基下的向量：`[x]ₑ`
- 新基矩阵：`P`（新基向量在标准基下的表示）
- 新基下的向量：`[x]ₚ`

**转换关系**：
```
[x]ₑ = P × [x]ₚ      # 从新基到标准基 

[x]ₚ = P⁻¹ × [x]ₑ    # 从标准基到新基
```
### 3. 与特征值分解的联系
#### 特征分解是特殊的基变换：A = PΛP⁻¹

其中：
* A：标准基下的矩阵
* Λ：特征基下的矩阵（对角阵）
* P：特征向量矩阵（从特征基到标准基的变换矩阵）

### 4. 相似矩阵
#### 对于同一线性变换在不同基下的表示：
A' = P⁻¹AP

A'和A是相似矩阵，它们有相同的：
- 特征值
- 行列式
- 迹
- 秩

### 5. 在ML中的应用
* PCA降维：将数据变换到主成分基
* 数据标准化/归一化
* 特征工程：创建更有意义的特征空间
* 神经网络：每一层都可以看作基变换

## 💻 代码实现
### 1. 基本基变换演示
```python
import numpy as np

print("=== 基变换基础演示 ===")
print()

# 定义两个不同的基
# 基1：标准基 e1=[1,0], e2=[0,1]
# 基2：新基 v1=[2,1], v2=[1,2]

# 新基向量在标准基下的表示
P = np.array([[2, 1],   # v1在标准基下的坐标
              [1, 2]])  # v2在标准基下的坐标

print("新基矩阵 P（列是新基向量）：")
print(P)
print()

# 一个向量在新基下的坐标
x_new = np.array([3, 2])  # 表示 3*v1 + 2*v2

# 转换到标准基
x_standard = P @ x_new
print(f"向量在新基下的坐标：[x]ₚ = {x_new}")
print(f"转换到标准基：[x]ₑ = P @ [x]ₚ = {x_standard}")
print()

# 反向转换
x_new_calculated = np.linalg.inv(P) @ x_standard
print(f"反向转换验证：[x]ₚ = P⁻¹ @ [x]ₑ = {x_new_calculated}")
print(f"是否相等？ {np.allclose(x_new, x_new_calculated)}")
```
### 2. 基变换与特征值分解
```python
print("\n" + "="*60)
print("=== 基变换与特征值分解 ===")
print()

# 回顾第9集的矩阵
A = np.array([[3, 1],
              [1, 3]])

# 计算特征值和特征向量
eigenvalues, eigenvectors = np.linalg.eig(A)

print(f"原始矩阵 A（在标准基下）：")
print(A)
print()

print(f"特征值 λ: {eigenvalues}")
print(f"特征向量矩阵 P（特征基在标准基下的表示）：")
print(eigenvectors)
print()

# 特征基下的矩阵（应该是对角阵）
Lambda = np.linalg.inv(eigenvectors) @ A @ eigenvectors
print(f"在特征基下的矩阵 Λ = P⁻¹AP：")
print(np.round(Lambda, 10))  # 四舍五入消除微小误差
print()

# 验证基变换公式
print("验证：A = PΛP⁻¹")
A_reconstructed = eigenvectors @ Lambda @ np.linalg.inv(eigenvectors)
print(f"重构的 A: \n{np.round(A_reconstructed, 10)}")
print(f"是否相等？ {np.allclose(A, A_reconstructed, atol=1e-10)}")
```
### 3. 相似矩阵：同一变换在不同基下的表示
```python
print("\n" + "="*60)
print("=== 相似矩阵：同一线性变换在不同基下的表示 ===")
print()

# 定义线性变换
def linear_transform(x):
    """一个简单的线性变换"""
    return np.array([[2, 1],
                     [1, 2]]) @ x

# 在标准基下，变换矩阵就是 A
A_standard = np.array([[2, 1],
                       [1, 2]])

# 选择一个新基
P = np.array([[1, 1],   # 新基向量 v1
              [1, -1]]) # 新基向量 v2

# 在新基下的变换矩阵
P_inv = np.linalg.inv(P)
A_new_basis = P_inv @ A_standard @ P

print(f"标准基下的变换矩阵 A：")
print(A_standard)
print()

print(f"新基矩阵 P：")
print(P)
print()

print(f"在新基下的变换矩阵 A' = P⁻¹AP：")
print(A_new_basis)
print()

# 验证：同一个向量在不同基下的变换结果一致
x_standard = np.array([3, 2])
print(f"标准基下的向量：x = {x_standard}")

# 在标准基下变换
y_standard = A_standard @ x_standard
print(f"标准基下变换结果：y = A·x = {y_standard}")

# 在新基下计算
x_new = P_inv @ x_standard
y_new = A_new_basis @ x_new
y_back_to_standard = P @ y_new

print(f"新基下的坐标：x' = P⁻¹·x = {x_new}")
print(f"新基下变换结果：y' = A'·x' = {y_new}")
print(f"转回标准基：y = P·y' = {y_back_to_standard}")
print(f"两种方法结果是否一致？ {np.allclose(y_standard, y_back_to_standard)}")
```
### 4. PCA中的基变换应用
```python
print("\n" + "="*60)
print("=== PCA中的基变换应用 ===")
print()

# 生成示例数据
np.random.seed(42)
n_samples = 100
X = np.zeros((n_samples, 3))

# 创建相关性数据
X[:, 0] = np.random.randn(n_samples) * 5  # 主方向
X[:, 1] = X[:, 0] * 0.7 + np.random.randn(n_samples) * 2  # 相关方向
X[:, 2] = np.random.randn(n_samples) * 1  # 噪声方向

print(f"原始数据形状：{X.shape}")
print(f"原始数据（前5行）：")
print(X[:5])
print()

# 中心化
X_mean = X.mean(axis=0)
X_centered = X - X_mean

# 计算协方差矩阵
cov_matrix = np.cov(X_centered.T)

print(f"协方差矩阵：")
print(cov_matrix)
print()

# PCA：找到新的基（主成分）
eigenvalues, eigenvectors = np.linalg.eig(cov_matrix)

# 按特征值大小排序
sorted_indices = np.argsort(eigenvalues)[::-1]
eigenvalues_sorted = eigenvalues[sorted_indices]
eigenvectors_sorted = eigenvectors[:, sorted_indices]

print(f"特征值（方差）：{eigenvalues_sorted}")
print(f"特征向量（主成分方向，新的基）：")
print(eigenvectors_sorted)
print()

# 基变换：将数据变换到主成分基
W = eigenvectors_sorted  # 变换矩阵
X_pca = X_centered @ W

print(f"在主成分基下的数据（前5行）：")
print(X_pca[:5])
print()

# 解释方差比例
total_variance = np.sum(eigenvalues_sorted)
explained_variance_ratio = eigenvalues_sorted / total_variance
cumulative_variance = np.cumsum(explained_variance_ratio)

print("方差解释比例：")
for i in range(3):
    print(f"  主成分{i+1}: {explained_variance_ratio[i]:.1%}")
print(f"  累积：{cumulative_variance}")
```
### 5. 基变换的性质验证
```python
print("\n" + "="*60)
print("=== 基变换重要性质验证 ===")
print()

A = np.array([[2, 1],
              [1, 2]])
P = np.array([[1, 2],
              [3, 4]])

P_inv = np.linalg.inv(P)
A_prime = P_inv @ A @ P

print("1. 特征值不变性验证：")
print(f"原始矩阵 A 的特征值: {np.linalg.eigvals(A)}")
print(f"相似矩阵 A' 的特征值: {np.linalg.eigvals(A_prime)}")
print(f"特征值是否相等? {np.allclose(np.linalg.eigvals(A), np.linalg.eigvals(A_prime))}")
print()

print("2. 行列式不变性验证：")
print(f"det(A) = {np.linalg.det(A):.4f}")
print(f"det(A') = {np.linalg.det(A_prime):.4f}")
print(f"行列式是否相等? {np.allclose(np.linalg.det(A), np.linalg.det(A_prime))}")
print()

print("3. 迹不变性验证：")
print(f"tr(A) = {np.trace(A):.4f}")
print(f"tr(A') = {np.trace(A_prime):.4f}")
print(f"迹是否相等? {np.allclose(np.trace(A), np.trace(A_prime))}")
```
## 🎯 关键理解
### 基变换在ML中的三大应用
```python
basis_change_applications = {
    "PCA降维": {
        "原理": "将数据变换到方差最大的正交基（主成分基）",
        "操作": "1. 中心化数据 2. 计算协方差矩阵 3. 特征值分解 4. 投影到主成分",
        "意义": "数据压缩、可视化、去除噪声"
    },
    "数据预处理": {
        "原理": "将数据变换到合适的基以便于学习",
        "操作": "标准化（零均值、单位方差）、归一化",
        "意义": "加速收敛、提高数值稳定性"
    },
    "特征工程": {
        "原理": "从原始特征空间变换到更有意义的特征空间",
        "操作": "多项式特征、交互特征、核方法",
        "意义": "提升模型性能、发现数据内在结构"
    }
}
```
## 📝 练习与思考
### 练习题
```python
print("=== 练习题 ===")
print()

# 练习1：基变换计算
print("练习1：基变换计算")
P = np.array([[1, 2], [3, 4]])
x_new = np.array([2, 3])
print(f"变换矩阵 P = \n{P}")
print(f"向量在新基下的坐标：[x]ₚ = {x_new}")
print("1. 计算在标准基下的坐标 [x]ₑ")
print("2. 验证反向变换的正确性")
print()

# 练习2：相似矩阵验证
print("练习2：相似矩阵验证")
A = np.array([[2, 1], [1, 2]])
P = np.array([[1, -1], [1, 1]])
print(f"矩阵 A = \n{A}")
print(f"基变换矩阵 P = \n{P}")
print("1. 计算 A' = P⁻¹AP")
print("2. 验证 A 和 A' 的特征值相同")
print("3. 验证 A 和 A' 的行列式相同")
print()

# 练习3：PCA基变换
print("练习3：PCA基变换")
data = np.array([[1, 2, 3],
                 [2, 4, 6],
                 [3, 6, 9],
                 [4, 8, 12]])
print(f"数据矩阵（4×3）: \n{data}")
print("1. 计算协方差矩阵的特征值和特征向量")
print("2. 将数据变换到主成分基")
print("3. 计算保留多少信息（方差解释比例）")
```
### 思考题
#### 为什么PCA选择特征向量作为新的基？

```text
提示：特征向量方向是协方差矩阵变化最大的方向
```
#### 基变换如何帮助简化矩阵运算？

```text
提示：思考在特征基下矩阵变为对角阵的情况
```
#### 在神经网络中，每一层可以看作怎样的基变换？

```text
提示：每一层将输入变换到新的特征空间
```
## 🚀 下一步学习建议
你已经掌握了：
✅ 基变换的基本概念
✅ 基变换与特征值分解的关系
✅ 相似矩阵的性质
✅ PCA中的基变换应用

### 第11集预告：抽象向量空间
```python
第11集联系 = {
    "与第10集的关系": "基变换在抽象向量空间中的推广",
    "在LLM中的应用": [
        "1. 理解高维嵌入空间（词向量空间）",
        "2. 函数空间与核方法",
        "3. 希尔伯特空间在ML中的应用"
    ],
    "重要性": "⭐⭐⭐⭐（理论重要，但可快速浏览）"
}
```
### 学习检查清单：
* 理解基变换公式 [x]ₑ = P × [x]ₚ 和 [x]ₚ = P⁻¹ × [x]ₑ

* 能实现向量的基变换

* 理解特征分解 A = PΛP⁻¹ 是一种基变换

* 知道PCA如何通过基变换实现降维

* 能用基变换解释数据预处理

#### 记住：基变换是理解特征空间、降维和特征工程的关键工具！ 🧠
